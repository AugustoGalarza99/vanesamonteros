// Loader.js
import React from 'react';
import './Loader.css'; // Archivo CSS para el estilo del loader

const Loader = () => {
    return (
        <div className="loader-overlay">
            <div className="loader"></div>
        </div>
    );
};

export default Loader;
